apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "warden.fullname" . }}-test-connection"
  labels:
    {{- include "warden.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "warden.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
