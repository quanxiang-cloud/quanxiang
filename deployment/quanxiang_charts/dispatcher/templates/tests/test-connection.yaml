apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dispatcher.fullname" . }}-test-connection"
  labels:
{{ include "dispatcher.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "dispatcher.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
